<view class="reviceWarp">
  <view class="reviceUser">
    <view class="addressTitle">
      <view>收货人:{{address.userName}}</view>
      <view>{{address.all}}</view>
    </view>
    <view class="revicePhone">{{address.telNumber}}</view>
  </view>
</view>
<view class="cartMain">
  <view class="cartTitle">支付</view>
  <view class="cartContent">
    <view class="cartItem" wx:if="{{value.checked}}" wx:for="{{cart}}" wx:for-index="key" wx:for-item="value" wx:key="value">
      <!-- 复选框 -->
      <!-- 图片 -->
      <view class="cartImg">
        <image class="" src="{{value.goods_small_logo}}" mode="widthFix" />
      </view>
      <view class="cartInfo">
        <view class="goodsName">{{value.goods_name}}</view>
        <view class="goodsPrice">
          <view class="goodsPriceYuan">￥{{value.goods_price}}</view>
          <view class="cartTool">
            <text class="cartNum">X {{value.num}}</text>
          </view>
        </view>
      </view>
    </view>
  </view>
</view>
<!-- 底部工具栏 -->
<view class="btmTool">
  <view class="totalPrice">
    <view class="totalMoney">
      合计:
      <text>￥{{totalPrice}}</text>
    </view>
    <view class="freightOrder">包含运费</view>
  </view>
  <view class="cartPay" bindtap="handlePay">支付({{totalNum}})</view>
</view>