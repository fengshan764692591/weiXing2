<!--components/Taps/Taps.wxml-->
<view class="tabs">
  <view class="titleTaps {{item.isActive?'active':''}}"
  wx:for="{{tabs}}"
  wx:key="id"
  bindtap="handleItemChange"
  data-index="{{index}}"
  >{{item.title}}</view>
</view>
<view class="tabsContent">
  <slot></slot>
</view>