.tabs {
  display: flex;
}
.tabs .titleTaps {
  flex: 1;
  display: flex;
  justify-content: center;
  align-items: center;
  color: #000;
  font-size: 30rpx;
  padding: 20rpx 0;
}
.tabs .active {
  color: var(--themeColor);
  border-bottom: 5rpx solid currentColor;
}
